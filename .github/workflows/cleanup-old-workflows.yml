name: 🧹 Cleanup Old Workflows

# Ce workflow temporaire sert à désactiver les anciens workflows
# Il sera supprimé après utilisation

on:
  workflow_dispatch:

jobs:
  cleanup-info:
    name: ℹ️ Information Nettoyage
    runs-on: ubuntu-latest
    
    steps:
    - name: 📢 Information
      run: |
        echo "🧹 NETTOYAGE WORKFLOWS ANCIENS"
        echo "================================"
        echo ""
        echo "ℹ️ Ce workflow temporaire informe que:"
        echo "• ci-cd-production.yml a été désactivé"
        echo "• ci-cd-simple.yml a été désactivé" 
        echo "• Seul ci-cd.yml est maintenant actif"
        echo ""
        echo "✅ Workflow principal actif: ci-cd.yml"
        echo "❌ Anciens workflows: désactivés et supprimés"
        echo ""
        echo "🎯 Résultat: Un seul workflow actif et fonctionnel"
        echo "🎊 Fini les conflits entre workflows !"
        echo ""
        echo "⚠️ Ce workflow sera supprimé après le nettoyage"